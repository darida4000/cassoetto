cmake_minimum_required(VERSION 2.8)
project( riciclo )
find_package( OpenCV REQUIRED )
add_executable( riciclo riciclo.cpp rs232.c )
target_link_libraries( riciclo  curl opencv_core opencv_imgproc opencv_video opencv_calib3d ${EXTERNAL_LIBS})
include_directories(/usr/incude)
